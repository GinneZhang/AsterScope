apiVersion: apps/v1
kind: Deployment
metadata:
  name: novasearch-api
  labels:
    app: novasearch
spec:
  replicas: 3
  selector:
    matchLabels:
      app: novasearch-api
  template:
    metadata:
      labels:
        app: novasearch-api
    spec:
      containers:
      - name: fastapi
        image: novasearch-api:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        envFrom:
        - secretRef:
            name: novasearch-secrets
        resources:
          requests:
            cpu: "500m"
            memory: "1Gi"
          limits:
            cpu: "2000m"
            memory: "4Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: novasearch-api-svc
spec:
  selector:
    app: novasearch-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer
